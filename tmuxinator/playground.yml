name: play
root: ~/projects/projrctsOfInterest/java-projects


windows:
  - interviews:
      layout: main-vertical
      # Synchronize all panes of this window, can be enabled before or after the pane commands run.
      # 'before' represents legacy functionality and will be deprecated in a future release, in favour of 'after'
      # synchronize: after
      panes:
        - cd interviews;vim .
  - LeetCodeAnimation: cd LeetCodeAnimation;vim .
  - design-patterns: cd java-design-patterns;vim .
  - Java: cd Java;vim .
  - system-design: cd ../python-projects;vim .
